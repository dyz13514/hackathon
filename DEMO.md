# 演示与录屏脚本

这份脚本面向评委和第一次接触项目的队友：用一个车间的真实决策链解释系统，而不是逐页念菜单。主视频建议 **8–10 分钟**；若比赛限时更短，保留表中标“主线”的镜头，API 和扩展能力放到答辩。实际数值以录制时界面为准，不预先承诺某个百分比一定改善。

## 录制前准备

1. 若要录制真实模型能力，在根目录 `.env` 中设 `LLM_MODE=LIVE`，配置兼容网关并重启后端；先检查 `/health` 的 `llm_mode=LIVE`，再用一条低成本请求单独彩排。模型请求失败会明确报错，录屏不要把确定性计算说成模型生成。`STUB`/`REPLAY` 仅用于离线测试或回放，页面会标明非 LIVE。
2. 打开 <http://localhost:5173> 和 <http://127.0.0.1:8000/api/docs>。浏览器写操作的登录口令是 `.env` 中的 `SESSION_SHARED_PASSWORD`。准备一个能看清表格和甘特图的屏幕分辨率，关闭无关通知。
3. **要从干净状态录制时**，先在后端 `/api/docs` 中调用 `POST /api/auth/login` 登录，再调用 `POST /api/demo/reset`。此操作会清空并重建业务表（订单、计划、导入批次等），**审计日志保留**；不要在有需要保留的数据的库上执行。重置后的输入版本应为 `1`，锚点为 `2026-03-02T08:00:00`。`python -m app.seed --demo` 只在缺少数据时填充，不能代替重置。
4. 录屏前试走一遍，记下本次生成的 `plan_id`、界面上的 FCFS 对比数值和 What-if 差值。ID 每次可能不同，不要把它写死在字幕里。

若 Schedule 提示 `PENDING_PLAN_EXISTS`，代表同一生产日已有待审批提案，不是模型或 AWS 故障。新版页面会展示现有计划和 Approval 入口；先审批或填写理由拒绝，再决定是否生成下一份。不要为了消除提示直接重置有价值的数据库。

固定 seed 的“今天”是 **2026-03-02 周一 08:00**，时域至 3 月 4 日。它含 14 个订单、5 台机器、8 名工人；`CNC-01` 独占深孔钻削能力，`MAT-STEEL-01` 初始存在缺口，`ORD-004` 交期裕度不足。讲述“明天停机”时指 **2026-03-03**。

## 主视频：一条完整决策链

| 时间 | 操作与镜头 | 讲述要点 / 画面证据 |
| --- | --- | --- |
| 0:00–0:40 | Dashboard：扫过订单、物料、机器、工人；停在 `CNC-01`、`MAT-STEEL-01` 和订单备注徽章。 | “小车间的订单、库存、机器和人员分散在不同表里；决策者要同时判断能不能做、何时做、改动会影响谁。”种子数据提供可重复的瓶颈与缺料场景。 |
| 0:40–1:55 | Schedule → **Generate today’s plan**。显示甘特图、`PARTIAL` / 不可排产列表、FCFS 基线。 | “内核先守硬约束，再在可行解里优化。”读一条 `blocking_reason` 和 `unblock_suggestion`，再读屏幕上的实际基线差值。`PARTIAL` 是把不能排的作业明示出来，不是偷偷删掉。 |
| 1:55–2:40 | Approval → 选择刚生成的待审计划，展示目标分项、输入版本，点 **Approve**。回 Dashboard 看 `ACTIVE`。 | “算法给的是提案；激活前会重校验硬约束、快照版本和并发版本。人保留最终决定权。” |
| 2:40–3:55 | What-if → 选 **Machine unavailable**，填 `CNC-01`、`2026-03-03T08:00:00`、`2026-03-03T14:00:00`，点 **Run simulation**。先展示迟交/不可排产差值，再回 Dashboard 看原 `ACTIVE` 未变。 | “先问如果瓶颈机停 6 小时会怎样；沙箱只读，不污染正式计划。”若录制中要走完整闭环，再点 **Generate formal proposal from this scenario**，转 Approval 审批新提案。审批前不要再创建另一个待审提案。 |
| 3:55–4:55 | Quote → 用 `PRD-BRACKET`、数量 `5`、期望交期 `2026-03-04 17:00` 试一次；接着 Insights 看机器容量和技能缺口。 | “询价和瓶颈分析也复用同一套确定性数据，不靠模型编一个交期。”展示“期望交期是否满足 / 最早可行完成 / 对已有订单影响”，不保证一定可承诺。 |
| 4:55–6:10 | Import & Mapping → 先打开 `demo_material_chinese_columns.csv`，再上传它；并排展示原表的 ERP 备注列、页面目标字段和 Validation 结果。该文件没有未解析单元格，直接点 **Confirm and import**；在 Dashboard 看物料来源，再到批次列表 **Revert**。 | “现实数据不会统一英文表头。系统先提出映射，整表确定性校验，用户确认才落库；每行有来源，整批能回滚。”本文件只更新既有物料。回滚后刷新 Dashboard 核对库存；`last_updated_at` 会变，不要说数据库逐字节复原。 |
| 6:10–7:15 | Risks → **Rescan**；Trace Viewer → 打开刚才的运行；Value Ledger → 显示 KPI、实测/预测成本与人工步骤。 | “风险、过程和价值都有可检查的记录。”只引用屏幕上有数值的指标；无记录时说明触发条件，而不是念目标值。 |
| 7:15–8:20 | 收尾回 Schedule / Approval，或用一张架构图总结。 | “确定性内核负责数字和约束；模型可以理解输入、生成叙述或在受限工具内编排重排，但不能绕过校验与审批。沙箱负责试错，Trace 负责追责。” |

主线里 What-if 有两种入口。结构化表单不依赖模型；自然语言入口在 `LIVE` 下由真实模型翻译，**执行前会显示结构化结果供确认**。以下句子可用于彩排，但不保证模型一定识别：

```text
CNC-01 明天上午停机 6 小时
把 ORD-009 的优先级改为紧急
将 MAT-STEEL-01 的可用库存改为 35
```

模型连接或输出失败时页面会报错，改用结构化表单；不会再用写死的词法把句子伪装成模型翻译。自然语言翻译结果中的时间应落在固定演示日期，而非电脑当前日期。

## 加拍镜头：覆盖主视频没有放下的能力

这些可以做成 2–3 分钟补充视频，或答辩时按提问展示。每一条都对应真实代码入口。

| 情节 | 可执行步骤 | 讲给评委的边界 |
| --- | --- | --- |
| 正式扰动与自主分级 | 从干净 seed 先生成并审批一份 `ACTIVE` 计划；在 `/api/docs` 登录后调用 `POST /api/disruptions`，请求体见下方；展示 `impact_class`、`autonomy_level`、`decisive_predicates`、受影响订单与新计划状态；到 Approval 处理新提案。 | 这条是**真实登记扰动并重排**，和只读 What-if 不同；前端目前没有扰动登记表单。若该生产日已有待审计划，API 会返回 `PENDING_PLAN_EXISTS`，应先处理旧提案。 |
| 陈旧提案防护 | 生成一份待审计划但先不审批；到 Import 上传 `demo_material_restock.csv` 并确认；回 Approval 点 Approve，展示旧输入版本与当前版本及重新生成入口。 | “输入一变，旧提案不能被直接激活。”之后可按链接重新生成。导入或回滚都会推进版本，镜头顺序要固定。 |
| 软偏好 | Preferences 创建一个针对现有订单/机器的 `AVOID_MACHINE_FOR_ORDER` 规则，先显示未启用，再启用并重新生成计划。 | 偏好影响评分和候选排序，**不能**放宽硬约束；其影响是否显著以本次计划的分项和作业为准。 |
| 计划解释与车间导出 | `/api/docs` 调用 `GET /api/plans/{plan_id}/explanation`；对 `ACTIVE` 计划调用 `POST /api/plans/{plan_id}/export?format=xlsx`。 | 解释与导出有 API，当前页面没有完整按钮。导出表包含排班、不可排产和元数据；下载后可打开核对。 |
| 安全与降级 | Dashboard 找 `ORD-013` 备注的 `untrusted`/注入提示；Trace 查看路径。另可用 `LLM_MODE=DISABLED` 重启，重复排产、审批和结构化 What-if。 | 不可信文本不会拥有系统指令权限；外部模型不可用时，确定性核心仍可用。模式切换应单独录一段，避免在主线中改变条件。 |

扰动 API 的演示请求体（`POST /api/disruptions`）：

```json
{
  "disruption": {
    "type": "MACHINE_BREAKDOWN",
    "machine_id": "CNC-01",
    "start_time": "2026-03-03T08:00:00",
    "end_time": "2026-03-03T14:00:00"
  }
}
```

## CSV 样例怎么选

样例目录：[`backend/app/seed/samples/`](backend/app/seed/samples/README.md)。每组场景最好先重置演示数据，避免上一轮导入、审批或风险缓解提案影响下一镜头。

| 文件 | 预期看点 | 注意 |
| --- | --- | --- |
| `demo_material_shortage.csv` | 钢材 35 kg、焊丝 10 kg：用来讨论缺料造成的风险和不可排产 | 导入会改正式库存；之后生成**新**计划再比较。 |
| `demo_material_restock.csv` | 钢材 520 kg、焊丝 60 kg：展示补货与重新排产 | 和初始 seed 对比，不要与缺料文件连续叠加后仍称“初始基线”。 |
| `demo_material_chinese_columns.csv` | 中文表头、额外 ERP 备注：展示映射确认和可回滚来源 | 额外列不会写入业务物料；当前单位也不会由 CSV 覆盖。 |
| `demo_new_workers.csv` | 工人名录的导入与回滚 | 新工人技能和班次为空，**不**用于演示新增产能。 |
| `dirty_orders.csv` | 混合格式、空表头、中文数量 | 只用于映射/校验；订单批次落库当前尚未实现。 |

导入页面不提供任意重选“源列”的编辑器。对于 `dirty_orders.csv` 这类必填列缺失或单元格不可解析的文件，应展示问题与人工介入的必要性，而非勾选确认框后提交。与此对应，评估套件在后端检查标注映射和输入安全。

## 团队讲述分工与创新证据

建议三人各负责一条连续线，不要每人重复项目简介：

1. **业务角色：**问题、固定车间数据、甘特图、FCFS 对比和不可排产解释。强调“量化影响”，念现场真实数值。
2. **系统角色：**What-if / 扰动重排、审批、陈旧提案。解释“模拟、提案、正式执行”三个不同状态，展示其中一次状态变化。
3. **可信 AI 角色：**导入映射、`untrusted`、Trace、降级、价值台账。解释模型负责哪些环节、工具与审批如何限定权限，并出示记录。

录制时把每个创新点都落到一块可见证据：**确定性**对应基线与同输入重放，**可控**对应审批和旧提案拦截，**可试错**对应沙箱差值，**可追溯**对应导入批次与 Trace，**业务价值**对应 Value Ledger 的实测口径。不要把需求文档里的目标值当成已经达到的录制结果。
